{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build-tests",
			"type": "shell",
			"command": "./build tests",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"group": "build"
		},
		{
			"label": "build-app",
			"type": "shell",
			"command": "./build app",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"group": "build"
		}
	]
}